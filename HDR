function setup() {
  createCanvas(400, 400);
 background("white")
}

function draw( ) {

      fill("red");
   stroke("blue")
   
  
 // console.log(mouseIsPressed);
 
 if (mouseIsPressed) {
    rect(mouseX, mouseY, 20, 35);
 }          
}
